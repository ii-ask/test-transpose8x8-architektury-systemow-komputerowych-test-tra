name: GitHub Classroom Workflow

on: [push]

jobs:
  build:
    name: Autograding
    runs-on: ubuntu-latest
    container: pwit81/ii-ask-asm:v2
    steps:
      - uses: actions/checkout@v2
      - uses: education/autograding@v1